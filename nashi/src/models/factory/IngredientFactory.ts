/*import {Ingredient} from "../Ingredient";
import {Recipe} from "../Recipe";
export class RecipeFactory {

  constructor() {
  }

  public createIngrediant(json: any)
  {
    var ingredients: Ingredient[] = [];

    for (var i = 0; i < json.length; i++) {
      let ingTemp: Ingredient = new Ingredient();
      artTemp.id = json["matches"][i].;
      //artTemp.alias = jsonArticle[i].alias;
      ingrediants.push(artTemp);
    }
    return ingrediants;
  }
}
*/
